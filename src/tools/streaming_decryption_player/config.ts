import type { ToolConfig } from '@/types/tool'

const config: ToolConfig = {
  name: '播放器',
  description: '播放器',
  icon: 'video',
  path: '/tools/streaming_decryption_player',
  category: 'System'
}

export default config
