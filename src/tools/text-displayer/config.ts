import type { ToolConfig } from '@/types/tool'

const config: ToolConfig = {
  name: 'Text Formatter',
  description: 'Format text, change case, and inspect content.',
  icon: 'Type',
  path: '/tools/text-displayer',
  category: 'Text'
}

export default config
