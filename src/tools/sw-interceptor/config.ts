import type { ToolConfig } from '@/types/tool'

const config: ToolConfig = {
  name: 'SW Interceptor',
  description: 'Demonstrates nested Service Worker with virtual path interception.',
  icon: 'Network',
  path: '/tools/sw-interceptor',
  category: 'System'
}

export default config
