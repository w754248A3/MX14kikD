import type { ToolConfig } from '@/types/tool'

const config: ToolConfig = {
  name: '文件混淆',
  description: '文件混淆',
  icon: 'File',
  path: '/tools/file_obfuscator',
  category: 'File'
}

export default config
